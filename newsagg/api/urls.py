from django.urls import path
from . import views

urlpatterns = [
    #path('news/', views.NewsListAPIView.as_view(), name='api_news_list'),
    #path('sources/', views.SourceListAPIView.as_view(), name='api_sources'),
]
